# 无 Mac 编译指南 - 用 GitHub Actions 在线编译 IPA

## 你需要什么

- 一台能上网的电脑（Windows / Linux / Mac 都行）
- 一个 GitHub 账号（没有就注册一个，免费）
- 一个 Apple ID（用于后面用 Sideloadly 自签安装）

## 操作步骤（5 分钟搞定）

### 第 1 步：注册 GitHub（已有跳过）

打开 https://github.com/signup 注册免费账号

### 第 2 步：创建新仓库

1. 打开 https://github.com/new
2. Repository name 填: `CallForwarder`
3. 选 **Public**（重要！Public 仓库 GitHub Actions 免费无限量）
4. 勾选 "Add a README file"
5. 点 Create repository

### 第 3 步：上传项目文件

**方式 A：网页上传（最简单）**

1. 在你的仓库页面点 "Add file" → "Upload files"
2. 把 CallForwarder 目录下的**所有文件和文件夹**拖进去
3. 注意要保留目录结构：
   ```
   .github/workflows/build-ipa.yml
   CallForwarder/CallForwarderApp.swift
   CallForwarder/ContentView.swift
   CallForwarder/ForwardingIntents.swift
   CallForwarder/Info.plist
   CallForwarderWidget/CallForwarderWidget.swift
   CallForwarderWidget/Info.plist
   Shared/SharedSettings.swift
   project.yml
   ```
4. 点 "Commit changes"

**方式 B：用 Git 命令**

```bash
# 下载项目文件后，在项目目录执行：
git init
git remote add origin https://github.com/你的用户名/CallForwarder.git
git add .
git commit -m "Initial commit"
git branch -M main
git push -u origin main
```

### 第 4 步：等待自动编译

1. 上传完成后，GitHub 会自动触发编译
2. 在仓库页面点 "Actions" 标签页
3. 会看到一个 "Build IPA" 的任务在运行
4. 等待约 3-5 分钟（绿色勾 = 成功，红色叉 = 失败）

### 第 5 步：下载 IPA

1. 编译成功后，点击那个绿色的任务
2. 拉到页面最底部，找到 "Artifacts" 区域
3. 点击 `CallForwarder-IPA` 下载
4. 解压后得到 `CallForwarder.ipa`

### 第 6 步：安装到 iPhone

下载 [Sideloadly](https://sideloadly.io/)（Windows/Mac 都有）：

1. 用 USB 线连接 iPhone 到电脑
2. 打开 Sideloadly
3. 输入你的 Apple ID
4. 拖入 `CallForwarder.ipa`
5. 点 Start 安装
6. iPhone 上信任开发者：设置 → 通用 → VPN与设备管理 → 信任你的 Apple ID

## 如果编译失败

1. 在 Actions 页面点击失败的任务
2. 查看日志找报错信息
3. 常见问题：
   - **Xcode 版本问题**：workflow 已配置 macOS-14 + Xcode 15.4
   - **文件路径错误**：确保目录结构和上面列出的一致
   - **Swift 语法错误**：检查文件内容是否完整

## 备注

- 编译出的 IPA 是**未签名**的，必须通过 Sideloadly / AltStore 自签后才能安装
- 免费 Apple ID 签名的 App 有效期 **7 天**，过期后需重新签名
- 付费开发者账号签名有效期 **1 年**
- 小组件功能需要 iOS 17.0+
